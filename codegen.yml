overwrite: true
config:
  # omitOperationSuffix: true
  skipTypeNameForRoot: true
generates:
  ./src/graphql/types/subgraph_types.ts:
    documents:
      - 'src/graphql/subgraph/*'
    schema: http://37.27.56.238:8000/subgraphs/name/graphprotocol/erc20-subgraph
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo" # To generate custom hooks per query
