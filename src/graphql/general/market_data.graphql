query MarketData ($denom: String) {
  communityPool: community_pool(order_by: {height: desc}, limit: 1) {
    coins
  }
  inflation: mint_params {
    params
  }
}

query Supplies {
  supply {
    coins
  }
}